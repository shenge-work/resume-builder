{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认权限：允许保存对话框（App 内导出落盘用），其余核心能力走自建命令不受 ACL 拦。",
  "windows": ["main"],
  "permissions": ["dialog:allow-save"]
}
