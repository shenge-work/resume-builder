# 构建产物（由 tools/build-single.js 生成，不入库）
dist/

# 个人简历数据（含真实信息，公开仓库必须隔离、不入库）
# data/ 整体不入库（私有实时数据源 resume.json 等）；公开示范模板已移到仓库根目录 template.json
data/

# 系统与编辑器
.DS_Store
Thumbs.db
*.swp

# 依赖与日志
node_modules/
npm-debug.log*
yarn-error.log*

# 编辑器配置
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# 本地 Agent / 工具状态（含本地记忆、运行日志，不入库）
.workbuddy/
