---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '0b564928-5785-46d2-a613-3216df9818e0'
  PropagateID: '0b564928-5785-46d2-a613-3216df9818e0'
  ReservedCode1: 'f27ee588-264c-4a55-8f64-2b1b6f9c761e'
  ReservedCode2: 'f27ee588-264c-4a55-8f64-2b1b6f9c761e'
---

# 设计规范 · 新粗野主义 × 潮流杂志 × 人才档案系统

模板已实现全部规范，本文档用于理解视觉基因、在调整时不破坏风格统一。任何改动先对照本清单。

## 1. 设计令牌（CSS :root）

| 令牌 | 值 | 用途 |
|---|---|---|
| `--paper` | `#F3EEE2` | 页面底色（米白偏暖） |
| `--card` | `#FFFCF4` | 卡片底色（比 paper 更亮一档） |
| `--ink` | `#141311` | 主文字/描边/阴影（近黑） |
| `--orange` | `#FF5A1E` | 主强调色（按钮/印章/数字） |
| `--blue` | `#2B4BE0` | 次强调色（英文小字/编号） |
| `--shadow` | `6px 6px 0 var(--ink)` | 硬阴影（无模糊偏移阴影，禁止 box-shadow 模糊） |
| `--border` | `3px solid var(--ink)` | 常规边框 |
| `--border-thick` | `4px solid var(--ink)` | 卡片级边框 |

**铁律**：全站零圆角、零渐变（全息渐变除外且已弃用）、零模糊阴影。立体感全部来自"纯色偏移阴影 + 多层堆叠卡片"。

## 2. 字体体系（三层，缺一不可）

| 层 | 字体 | 用途 |
|---|---|---|
| 中文标题 | Noto Sans SC **900** | 姓名大字、各板块标题、项目名、公司名 |
| 英文展示 | **Share Tech Mono**（等宽科技字） | 英文名、区块英文小标、P-01 编号、统计数字、年份、JY. 徽标 |
| 功能标签 | Space Mono 700 | 小标签、坐标、编号前缀、时间戳 |

中文 900 字重必须显式声明（模板中 `--font-display` 已带 `font-weight: 900`）。等宽字无中文字形，中文一律不进等宽字体层（曾在"8 项"上翻车：中文回退系统字体造成割裂）。

## 3. 布局语言

- **首屏**：左列（超大中文名 + 故障动画 + 英文名独立一行在下 + 职位 + 简介 + 按钮）/ 右列（三层堆叠立体档案卡：橙色底卡 -2.4°、蓝色底卡 +1.6°、主卡黑色硬阴影）
- **姓名排版**：中文 `clamp(96px, 15vw, 200px)`，英文 `clamp(40px, 5.4vw, 80px)` 蓝描边空心，英文名**独立成行**放中文名下方（禁止叠压，曾因叠压返工）
- **档案卡字段**：2 列 dl 网格，dt 是等宽小字标签（含英文如 `姓名 NAME`），dd 是粗体值；关键值用橙/蓝着色（GPA 橙、方向蓝）
- **区块头**：`SEC.01` 黑底等宽编号 + 中文大标题 + 英文蓝色等宽小标 + 伸缩横线 + 右侧代码注释风格坐标（如 `ARCHIVE//BIO-GRAPHY`）
- **隔行区块**：`.section-tint` 加 40px 网格纹理底（rgba 墨色 5%）
- **项目卡**：16:9 封面 + 编号徽章（P-01）+ 悬停时上浮 10px、旋转 ±1.4°、错位硬阴影翻转方向、半透明遮罩浮出"查看详情"按钮；奇偶卡初始各带 ±0.6° 倾斜
- **滚动横幅 marquee**：橙底白字，`✦` 分隔，26s 线性循环

## 4. 签名元素清单（缺了就不像）

- 状态灯：9px 圆点带 2px 黑描边 + 闪烁动画（导航"求职中"、档案卡"ACTIVE"）
- 印章：圆形双线橙描边"已审核 VERIFIED"，旋转 12°，叠在关于我照片右上
- 条形码：CSS `repeating-linear-gradient` 手绘，档案卡底部 + 页脚
- 像素箭头：`▶▶` / `▼`，等宽字体，按钮与滚动提示里
- 坐标：`N30.27° / E120.16°`（按用户实际城市换），首屏四角 + 联系方式 + 页脚
- 档案编号：`JY-2026-0117` 格式 = 姓氏缩写-年份-序号，首屏角落 + 档案卡 + 简历页
- 故障 Glitch 动画：仅首屏中文姓名，蓝层固定向左、橙层固定向右错位（方向必须镜像才明显），3.6s 周期 steps(1)，86%~96% 区间闪现，`prefers-reduced-motion` 时禁用

## 5. 组件规范速查

- 按钮：黑边硬阴影，hover 位移 -2px,-2px + 阴影扩大，active 归零；实心橙 = 主操作
- 标签 `.tag`：等宽 12px + 2px 黑边；橙底白字 = 高亮，蓝底白字 = 次高亮
- 模态框：项目详情全量数据（做什么/技术栈/数据三块），ESC 关闭 + 遮罩点击关闭 + 焦点管理
- 技能条：橙色斜纹填充（偶数行蓝色），IntersectionObserver 触发宽度动画
- 统计数字：等宽字体 + 1.1s 滚动动画，`data-count` 驱动
- 证书卡：31:22 比例封面 + 角落 CERT-01 印章标签 + hover 上浮旋转

## 6. 响应式断点

- `1024px`：导航收进汉堡菜单、首屏单列（档案卡下移）、关于我单列、技能单列
- `680px`：区块边距 18px、统计 2×2、项目单列、教育统计单列、模态框统计单列、CTA 按钮全宽
- 验收底线：390px / 360px 宽度无横向滚动（装饰性溢出元素必须有 `overflow: hidden` 容器）

## 7. 无障碍底线

- 模态框 `role="dialog"` + `aria-modal` + 焦点圈管理 + ESC
- 项目卡 `tabindex="0"` + `role="button"` + Enter/Space 触发
- 装饰性元素 `aria-hidden="true"`
- `prefers-reduced-motion` 全局禁动画

> AI生成